import { healthcheck } from "@/controllers/healthcheck";
import { installments } from "@/controllers/installments";

const controllers = {
  installments,
  healthcheck,
};

export default controllers;
